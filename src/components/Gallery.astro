---
// Gallery Section — La Casa de Kuki
// Imágenes reales organizadas por departamento y espacios

const tabs = [
  { id: "todos", label: "Todos" },
  { id: "inti", label: "Depto Inti ☀️" },
  { id: "killa", label: "Depto Killa 🌙" },
  { id: "exterior", label: "Exterior" },
];

const galleryItems = [
  // Departamento Inti
  {
    src: "/images/inti/IMG_3150.PNG",
    alt: "Depto Inti — ambiente principal cálido y confortable",
    label: "Depto Inti",
    category: "inti",
    span: "col-span-2",
  },
  {
    src: "/images/inti/IMG_3151.PNG",
    alt: "Depto Inti — dormitorio con ropa de cama de calidad",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3152.PNG",
    alt: "Depto Inti — baño privado",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3153.PNG",
    alt: "Depto Inti — espacio interior luminoso",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3154.PNG",
    alt: "Depto Inti — detalles del ambiente",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3155.PNG",
    alt: "Depto Inti — kit de infusiones regionales y pava eléctrica",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3156.PNG",
    alt: "Depto Inti — vista del living",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3157.PNG",
    alt: "Depto Inti — rincón de descanso",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3158.PNG",
    alt: "Depto Inti — ambiente equipado",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  {
    src: "/images/inti/IMG_3159.PNG",
    alt: "Depto Inti — detalle final del espacio",
    label: "Depto Inti",
    category: "inti",
    span: "",
  },
  // Departamento Killa
  {
    src: "/images/killa/IMG_3160.PNG",
    alt: "Depto Killa — ambiente principal cálido y confortable",
    label: "Depto Killa",
    category: "killa",
    span: "col-span-2",
  },
  {
    src: "/images/killa/IMG_3161.PNG",
    alt: "Depto Killa — dormitorio con ropa de cama de calidad",
    label: "Depto Killa",
    category: "killa",
    span: "",
  },
  {
    src: "/images/killa/IMG_3162.PNG",
    alt: "Depto Killa — baño privado",
    label: "Depto Killa",
    category: "killa",
    span: "",
  },
  {
    src: "/images/killa/IMG_3163.PNG",
    alt: "Depto Killa — espacio interior luminoso",
    label: "Depto Killa",
    category: "killa",
    span: "",
  },
  {
    src: "/images/killa/IMG_3164.PNG",
    alt: "Depto Killa — detalles del ambiente",
    label: "Depto Killa",
    category: "killa",
    span: "",
  },
  {
    src: "/images/killa/IMG_3165.PNG",
    alt: "Depto Killa — kit de infusiones y comodidades",
    label: "Depto Killa",
    category: "killa",
    span: "",
  },
  {
    src: "/images/killa/IMG_3166.PNG",
    alt: "Depto Killa — rincón de descanso",
    label: "Depto Killa",
    category: "killa",
    span: "",
  },
  {
    src: "/images/killa/1000287697.JPG",
    alt: "Depto Killa — vista general del departamento",
    label: "Depto Killa",
    category: "killa",
    span: "",
  },
  // Exterior
  {
    src: "/images/exterior/1000472082.JPG",
    alt: "Exterior — frente de La Casa de Kuki en Purmamarca",
    label: "Exterior",
    category: "exterior",
    span: "col-span-2",
  },
  {
    src: "/images/exterior/1000367568.JPG",
    alt: "Exterior — jardín y estacionamiento privado dentro del predio",
    label: "Exterior",
    category: "exterior",
    span: "",
  },
];
---

<section id="galeria" class="gallery-section">
  <div class="section-wrapper">
    <!-- Header -->
    <div class="gallery-header">
      <p class="gallery-eyebrow">Conocé nuestros espacios</p>
      <h2 class="section-title">
        Un rincón único<br /><em>en la Quebrada</em>
      </h2>
      <div class="section-divider"><span>✦</span></div>
      <p class="section-subtitle">
        Departamentos Inti y Killa, quincho compartido, estacionamiento privado
        y vistas a los cerros — todo pensado para que llegués y
        te sientas en casa.
      </p>
    </div>

    <!-- Filtros por categoría -->
    <div
      class="gallery-tabs"
      role="tablist"
      aria-label="Filtrar galería por espacio"
    >
      {
        tabs.map((tab) => (
          <button
            class={`gallery-tab ${tab.id === "todos" ? "active" : ""}`}
            role="tab"
            aria-selected={tab.id === "todos" ? "true" : "false"}
            data-filter={tab.id}
            id={`tab-${tab.id}`}
          >
            {tab.label}
          </button>
        ))
      }
    </div>

    <!-- Grid de galería -->
    <div class="gallery-grid" id="gallery-grid" role="list">
      {
        galleryItems.map((item, index) => (
          <div
            class={`gallery-item ${item.span}`}
            role="listitem"
            data-category={item.category}
            style={`animation-delay: ${index * 0.05}s`}
          >
            <button
              class="gallery-btn"
              aria-label={`Ver foto: ${item.alt}`}
              data-src={item.src}
              data-alt={item.alt}
              data-index={index}
            >
              <img
                src={item.src}
                alt={item.alt}
                loading="lazy"
                decoding="async"
                class="gallery-img"
              />
              <div class="gallery-overlay" aria-hidden="true">
                <div class="gallery-overlay-content">
                  <svg
                    width="28"
                    height="28"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                  >
                    <path d="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z" />
                  </svg>
                  <span>Ver foto</span>
                </div>
              </div>
            </button>
            <div class="gallery-label" aria-hidden="true">
              <span>{item.label}</span>
            </div>
          </div>
        ))
      }
    </div>

    <!-- Contador de fotos -->
    <p class="gallery-count" id="gallery-count" aria-live="polite"></p>
  </div>

  <!-- Lightbox -->
  <div
    id="lightbox"
    class="lightbox"
    role="dialog"
    aria-modal="true"
    aria-label="Galería de fotos"
    hidden
  >
    <div class="lightbox-backdrop" id="lightbox-backdrop"></div>
    <div class="lightbox-content">
      <button
        class="lightbox-close"
        id="lightbox-close"
        aria-label="Cerrar galería"
      >
        <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
          <path
            d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"
          ></path>
        </svg>
      </button>
      <img id="lightbox-img" src="" alt="" class="lightbox-img" />
      <p id="lightbox-caption" class="lightbox-caption"></p>
      <div class="lightbox-counter">
        <span id="lightbox-counter-text"></span>
      </div>
      <div class="lightbox-nav">
        <button
          id="lightbox-prev"
          class="lightbox-nav-btn"
          aria-label="Foto anterior"
        >
          <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
            <path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"></path>
          </svg>
        </button>
        <button
          id="lightbox-next"
          class="lightbox-nav-btn"
          aria-label="Foto siguiente"
        >
          <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
            <path d="M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>
</section>

<script>
  // --- Tab filtering ---
  const tabs = document.querySelectorAll<HTMLButtonElement>(".gallery-tab");
  const allItems = document.querySelectorAll<HTMLElement>(".gallery-item");
  const galleryCount = document.getElementById("gallery-count");

  function getVisibleItems(): HTMLElement[] {
    return Array.from(allItems).filter(
      (item) => !item.classList.contains("hidden"),
    );
  }

  function updateCount() {
    const visible = getVisibleItems().length;
    const total = allItems.length;
    if (galleryCount) {
      galleryCount.textContent =
        visible === total
          ? `Mostrando ${total} fotos`
          : `Mostrando ${visible} de ${total} fotos`;
    }
  }

  function filterGallery(filter: string) {
    allItems.forEach((item) => {
      const cat = item.dataset.category || "";
      const show = filter === "todos" || cat === filter;
      if (show) {
        item.classList.remove("hidden");
        item.style.display = "";
      } else {
        item.classList.add("hidden");
        item.style.display = "none";
      }
    });
    updateCount();
  }

  tabs.forEach((tab) => {
    tab.addEventListener("click", () => {
      tabs.forEach((t) => {
        t.classList.remove("active");
        t.setAttribute("aria-selected", "false");
      });
      tab.classList.add("active");
      tab.setAttribute("aria-selected", "true");
      filterGallery(tab.dataset.filter || "todos");
    });
  });

  updateCount();

  // --- Lightbox ---
  const lightbox = document.getElementById("lightbox") as HTMLElement | null;
  const lightboxImg = document.getElementById(
    "lightbox-img",
  ) as HTMLImageElement | null;
  const lightboxCaption = document.getElementById("lightbox-caption");
  const lightboxClose = document.getElementById("lightbox-close");
  const lightboxBackdrop = document.getElementById("lightbox-backdrop");
  const lightboxPrev = document.getElementById("lightbox-prev");
  const lightboxNext = document.getElementById("lightbox-next");
  const lightboxCounterText = document.getElementById("lightbox-counter-text");

  const galleryButtons =
    document.querySelectorAll<HTMLButtonElement>(".gallery-btn");
  let currentIndex = 0;
  const items = Array.from(galleryButtons).map((btn) => ({
    src: btn.dataset.src || "",
    alt: btn.dataset.alt || "",
  }));

  function getActiveButtons(): HTMLButtonElement[] {
    return Array.from(galleryButtons).filter((btn) => {
      const parent = btn.closest(".gallery-item") as HTMLElement | null;
      return parent && !parent.classList.contains("hidden");
    });
  }

  function openLightbox(index: number) {
    currentIndex = index;
    if (!lightbox || !lightboxImg || !lightboxCaption) return;
    lightboxImg.src = items[index].src;
    lightboxImg.alt = items[index].alt;
    lightboxCaption.textContent = items[index].alt;
    updateLightboxCounter();
    lightbox.hidden = false;
    document.body.style.overflow = "hidden";
    lightbox.focus();
  }

  function closeLightbox() {
    if (!lightbox) return;
    lightbox.hidden = true;
    document.body.style.overflow = "";
  }

  function updateLightboxCounter() {
    const active = getActiveButtons();
    const pos = active.findIndex(
      (btn) => parseInt(btn.dataset.index || "0") === currentIndex,
    );
    if (lightboxCounterText) {
      lightboxCounterText.textContent = `${pos + 1} / ${active.length}`;
    }
  }

  function navigate(direction: number) {
    const active = getActiveButtons();
    const pos = active.findIndex(
      (btn) => parseInt(btn.dataset.index || "0") === currentIndex,
    );
    const nextPos = (pos + direction + active.length) % active.length;
    currentIndex = parseInt(active[nextPos].dataset.index || "0");
    if (!lightboxImg || !lightboxCaption) return;
    lightboxImg.src = items[currentIndex].src;
    lightboxImg.alt = items[currentIndex].alt;
    lightboxCaption.textContent = items[currentIndex].alt;
    updateLightboxCounter();
  }

  galleryButtons.forEach((btn, i) => {
    btn.addEventListener("click", () => openLightbox(i));
  });

  lightboxClose?.addEventListener("click", closeLightbox);
  lightboxBackdrop?.addEventListener("click", closeLightbox);
  lightboxPrev?.addEventListener("click", () => navigate(-1));
  lightboxNext?.addEventListener("click", () => navigate(1));

  document.addEventListener("keydown", (e) => {
    if (lightbox?.hidden) return;
    if (e.key === "Escape") closeLightbox();
    if (e.key === "ArrowLeft") navigate(-1);
    if (e.key === "ArrowRight") navigate(1);
  });

  // Intersection Observer para animaciones de entrada
  const galleryItemEls = document.querySelectorAll(".gallery-item");
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          (entry.target as HTMLElement).style.opacity = "1";
          (entry.target as HTMLElement).style.transform = "translateY(0)";
        }
      });
    },
    { threshold: 0.05 },
  );

  galleryItemEls.forEach((item) => {
    (item as HTMLElement).style.opacity = "0";
    (item as HTMLElement).style.transform = "translateY(20px)";
    (item as HTMLElement).style.transition =
      "opacity 0.5s ease, transform 0.5s ease";
    observer.observe(item);
  });
</script>

<style>
  .gallery-section {
    padding: 6rem 0;
    background-color: var(--color-blanco-calido);
  }

  .gallery-header {
    text-align: center;
    margin-bottom: 2.5rem;
  }

  .gallery-eyebrow {
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 0.8125rem;
    color: var(--color-terracota);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin-bottom: 0.75rem;
  }

  .section-title em {
    font-style: italic;
    color: var(--color-terracota);
    display: block;
  }

  /* Tabs de filtro */
  .gallery-tabs {
    display: flex;
    justify-content: center;
    gap: 0.625rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .gallery-tab {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 0.875rem;
    padding: 0.5rem 1.25rem;
    border-radius: 9999px;
    border: 1.5px solid var(--color-arena);
    background: transparent;
    color: var(--color-carbon-light);
    cursor: pointer;
    transition: all 0.22s ease;
    letter-spacing: 0.02em;
  }

  .gallery-tab:hover {
    border-color: var(--color-terracota);
    color: var(--color-terracota);
    background: rgba(193, 96, 58, 0.06);
  }

  .gallery-tab.active {
    background: var(--color-terracota);
    border-color: var(--color-terracota);
    color: white;
    box-shadow: 0 4px 14px rgba(193, 96, 58, 0.3);
  }

  /* Grid */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .gallery-item {
    position: relative;
    border-radius: 1rem;
    overflow: hidden;
    aspect-ratio: 4/3;
  }

  .gallery-item.col-span-2 {
    grid-column: span 2;
  }

  .gallery-btn {
    width: 100%;
    height: 100%;
    border: none;
    padding: 0;
    cursor: pointer;
    background: none;
    display: block;
    position: relative;
  }

  .gallery-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
    display: block;
  }

  .gallery-btn:hover .gallery-img {
    transform: scale(1.06);
  }

  .gallery-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      160deg,
      rgba(122, 75, 46, 0) 40%,
      rgba(122, 75, 46, 0.7) 100%
    );
    opacity: 0;
    transition: opacity 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .gallery-btn:hover .gallery-overlay {
    opacity: 1;
  }

  .gallery-overlay-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: white;
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 0.9rem;
    letter-spacing: 0.04em;
  }

  .gallery-label {
    position: absolute;
    bottom: 0.875rem;
    left: 0.875rem;
  }

  .gallery-label span {
    background-color: rgba(250, 247, 242, 0.92);
    color: var(--color-marron);
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 0.8125rem;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  /* Contador */
  .gallery-count {
    text-align: center;
    font-family: var(--font-body);
    font-size: 0.8125rem;
    color: var(--color-carbon-light);
    margin-top: 0.5rem;
    opacity: 0.7;
  }

  /* Lightbox */
  .lightbox {
    position: fixed;
    inset: 0;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .lightbox[hidden] {
    display: none;
  }

  .lightbox-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(20, 12, 6, 0.94);
    backdrop-filter: blur(6px);
  }

  .lightbox-content {
    position: relative;
    z-index: 1;
    max-width: 90vw;
    max-height: 90vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .lightbox-img {
    max-width: 100%;
    max-height: 72vh;
    object-fit: contain;
    border-radius: 0.75rem;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  }

  .lightbox-caption {
    color: rgba(255, 255, 255, 0.75);
    font-family: var(--font-body);
    font-size: 0.875rem;
    text-align: center;
    max-width: 600px;
    margin: 0;
  }

  .lightbox-counter {
    color: rgba(255, 255, 255, 0.5);
    font-family: var(--font-body);
    font-size: 0.8125rem;
    letter-spacing: 0.05em;
  }

  .lightbox-close {
    position: absolute;
    top: -3rem;
    right: 0;
    background: rgba(255, 255, 255, 0.15);
    border: none;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    cursor: pointer;
    transition: background 0.2s;
  }

  .lightbox-close:hover {
    background: rgba(255, 255, 255, 0.3);
  }

  .lightbox-nav {
    display: flex;
    gap: 1rem;
  }

  .lightbox-nav-btn {
    background: rgba(255, 255, 255, 0.15);
    border: none;
    border-radius: 50%;
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    cursor: pointer;
    transition: background 0.2s;
  }

  .lightbox-nav-btn:hover {
    background: rgba(255, 255, 255, 0.3);
  }

  /* Hidden items (filter) */
  .gallery-item.hidden {
    display: none;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .gallery-grid {
      grid-template-columns: 1fr 1fr;
    }
    .gallery-item.col-span-2 {
      grid-column: span 2;
    }
  }

  @media (max-width: 480px) {
    .gallery-grid {
      grid-template-columns: 1fr;
    }
    .gallery-item.col-span-2 {
      grid-column: span 1;
    }
    .gallery-tabs {
      gap: 0.5rem;
    }
    .gallery-tab {
      font-size: 0.8125rem;
      padding: 0.4rem 0.875rem;
    }
  }
</style>
